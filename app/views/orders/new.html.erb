<%= render :partial => '/shared/sidebar2' %>
          <!-- /.container -->

    <div id="page-wrapper">

            <div class="container-fluid">

                <!-- Page Heading -->
                    <div class="row">
                    <div class="col-lg-12" style="margin-top: 60px;">
                            <% if notice %>
                          <p class="alert alert-info alert-dismissable"><%= notice %></p>
                          <% end %>
                      <% if alert %>
                       <p class="alert alert-danger alert-dismissable"><%= alert %></p>
                       <% end %>
                    </div>
                    </div>
                    <div class="row">
                    <div class="col-lg-12">
                        <h1 class="page-header">
                            Order <small>Add an order for authorisation</small>
                        </h1>
                        <ol class="breadcrumb">
                            <li class="active">
                                <i class="fa fa-dashboard"></i> New
                            </li>
                        </ol>
                    </div>
                </div>
      <div class="row">
            <!-- A vertical nav-bar -->
              <!-- A table -->
            <div class="col-lg-12">
       
                <div class="col-lg-6">

                   <!--  <%- model_class = Order -%> -->
                        <div class="page-header">
                          <!-- <h1><%=t '.title', :default => [:'helpers.titles.new', 'New %{model}'], :model => model_class.model_name.human.titleize %></h1> -->
                        </div>

                        <%= render :partial => '/shared/form1' %>

                 </div>

               
            </div>
        </div>
</div>
